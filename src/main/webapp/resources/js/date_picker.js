$("input[date-picker]").each(

    $(this).datetimepicker({
        format: 'DD-MM-YYYY',
        useCurrent: false
    });
)

$("input[date-picker]").datetimepicker({
        format: 'DD-MM-YYYY',
        useCurrent: false
    });